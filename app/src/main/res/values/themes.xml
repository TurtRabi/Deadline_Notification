<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NotificationDeadline" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">#F8FEEC</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NotificationDeadline" parent="Base.Theme.NotificationDeadline" />

    <style name="Theme.NotificationDeadline.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">#F8FEEC</item>
        <item name="android:statusBarColor">#F8FEEC</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item>
    </style>

</resources>